<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GameCollection" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GameCollection" parent="Base.Theme.GameCollection" />

    <!-- Custom Button Style -->
    <style name="CustomButtonStyle">
        <item name="android:background">@drawable/paranoia_button</item>
        <item name="android:backgroundTint">@null</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>
